[meta]
title     = "ALTER TYPE … RENAME VALUE — enum label rename"
authoring = "objects"
spec_refs = ["types.enum.rename_value"]

[pg]
min = 14
max = 17

[expect.diff]
contains = [
  "types.app.status",
]

[expect.plan]
steps = 1
