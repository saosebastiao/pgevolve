[meta]
title     = "COMMENT ON TYPE — add comment to an existing enum"
authoring = "objects"
spec_refs = ["types.enum.comment"]

[pg]
min = 14
max = 17

[expect.diff]
contains = [
  "types.app.role",
]

[expect.plan]
steps = 1
