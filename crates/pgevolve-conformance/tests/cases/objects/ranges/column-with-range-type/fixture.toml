[meta]
title     = "Table column whose type references a user-defined range type — exercises Range -> Table dep edge"
authoring = "objects"
spec_refs = ["types.range.create", "tables.create"]

[pg]
min = 14
max = 18

[expect.plan]
steps = 2
order = [
  "app.int_window < app.reservations",
]
