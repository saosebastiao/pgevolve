[meta]
title     = "CREATE OR REPLACE FUNCTION — body change emits one step"
authoring = "objects"
spec_refs = ["functions.replace_body"]

[pg]
min = 14
max = 17

[expect.diff]
contains = [
  "functions.app.double(integer)",
]

[expect.plan]
steps = 1
