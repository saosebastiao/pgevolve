[meta]
title     = "CREATE FUNCTION — RETURNS TABLE plpgsql with RETURN QUERY"
authoring = "objects"
spec_refs = ["functions.returns_table"]

[pg]
min = 14
max = 18

[expect.diff]
contains = [
  "functions.app.get_pairs()",
]

[expect.plan]
steps = 1
