[meta]
title     = "ALTER DEFAULT PRIVILEGES IN SCHEMA — GRANT SELECT ON TABLES TO readers"
authoring = "objects"
spec_refs = ["grants.default_privs.in_schema"]

[pg]
min = 14
max = 17

[expect.plan]
steps = 1
