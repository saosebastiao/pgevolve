[meta]
title     = "GRANT ALL ON TABLE — expands to individual privileges in the catalog"
authoring = "objects"
spec_refs = ["grants.table.all"]

[pg]
min = 14
max = 17

# GRANT ALL expands to 7 privileges (SELECT, INSERT, UPDATE, DELETE, TRUNCATE,
# REFERENCES, TRIGGER); each is a separate Grant step in the diff.
[expect.plan]
steps = 7
