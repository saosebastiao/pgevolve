[meta]
title     = "CREATE RECURSIVE VIEW — recursive view over a table (CTE name == view name)"
authoring = "objects"
spec_refs = ["views.create"]

[pg]
min = 14
max = 18

[expect.diff]
contains = [
  "views.app.node_tree",
]
