[meta]
title     = "ADD CONSTRAINT CHECK — NOT VALID + VALIDATE pattern (online strategy)"
authoring = "objects"
spec_refs = ["objects.table.add_constraint"]

[pg]
min = 14
max = 17

[expect.diff]
contains = [
  "products_quantity_positive",
]

[expect.plan]
steps        = 2
rewrites_used = ["check_not_valid_then_validate"]
