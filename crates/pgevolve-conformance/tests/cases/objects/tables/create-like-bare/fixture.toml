[meta]
title     = "CREATE TABLE (LIKE base) — bare clone copies columns only"
authoring = "objects"
spec_refs = ["objects.table.create"]

[pg]
min = 14
max = 17

[expect.diff]
contains = ["app.clone"]

[expect.plan]
steps = 1
