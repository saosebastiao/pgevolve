[meta]
title     = "CREATE TABLE WITH multiple reloptions — single SET step"
authoring = "objects"
spec_refs = ["objects.reloptions.table"]

[pg]
min = 14
max = 17

# The differ batches all three keys into a single ALTER TABLE SET step.
[expect.plan]
steps = 1
