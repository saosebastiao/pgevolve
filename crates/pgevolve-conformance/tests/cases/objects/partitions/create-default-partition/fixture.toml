[meta]
title     = "Partitioning — create LIST parent with a regular partition and a DEFAULT partition"
authoring = "objects"
spec_refs = ["partitioning.create"]

[pg]
min = 14
max = 17

[expect.diff]
contains = [
  "app.logs",
]

[expect.plan]
steps = 3
