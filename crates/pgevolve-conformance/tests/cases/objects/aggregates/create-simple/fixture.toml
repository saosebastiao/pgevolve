[meta]
title     = "CREATE AGGREGATE — minimal SFUNC + STYPE over a managed plpgsql state function"
authoring = "objects"
spec_refs = ["objects.aggregate"]

[pg]
min = 14
max = 18

[expect.plan]
steps = 1
