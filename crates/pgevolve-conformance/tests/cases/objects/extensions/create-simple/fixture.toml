[meta]
title     = "CREATE EXTENSION — bare CREATE (no schema, no version)"
authoring = "objects"
spec_refs = ["extensions.create"]

[pg]
min = 14
max = 17

[expect.diff]
contains = [
  "extensions.pgcrypto",
]

[expect.plan]
steps = 1
