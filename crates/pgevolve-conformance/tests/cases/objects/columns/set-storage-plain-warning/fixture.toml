[meta]
title     = "SET STORAGE PLAIN — downgrade fires storage-downgrade-not-retroactive advisory warning"
authoring = "objects"
spec_refs = ["objects.column.storage", "lint.storage-downgrade"]

[pg]
min = 14
max = 17

[expect.diff]
contains = [
  "app.docs.columns.body",
]

[expect.plan]
steps = 1

[expect.advisory]
rule_ids = ["storage-downgrade-not-retroactive"]
