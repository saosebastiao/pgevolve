[meta]
title     = "ADD COLUMN with DEFAULT — emits ALTER TABLE ADD COLUMN with default clause"
authoring = "objects"
spec_refs = ["objects.column.add"]

[pg]
min = 14
max = 17

[expect.diff]
contains = [
  "app.products.columns.quantity",
]

[expect.plan]
steps = 1
