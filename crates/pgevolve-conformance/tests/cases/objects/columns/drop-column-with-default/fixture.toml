[meta]
title     = "DROP COLUMN with DEFAULT — requires intent approval"
authoring = "objects"
spec_refs = ["objects.column.drop"]

[pg]
min = 14
max = 17

[expect.diff]
contains = [
  "app.orders.columns.status",
]

[expect.plan]
steps      = 1
minimality = false  # DROP COLUMN is irreversible; post-apply re-plan is empty

[[expect.intent]]
kind   = "drop_column"
target = "app.orders"
reason_contains = []
