[meta]
title     = "ALTER DOMAIN … ADD CONSTRAINT CHECK — add check to existing domain"
authoring = "objects"
spec_refs = ["types.domain.add_check"]

[pg]
min = 14
max = 17

[expect.diff]
contains = [
  "types.app.score",
]

[expect.plan]
steps = 1
