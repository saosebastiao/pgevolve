[meta]
title     = "DROP TYPE (enum) requires an approved intent"
authoring = "intent"
spec_refs = ["types.enum.drop"]

[expect.diff]
contains = ["app.status"]

[expect.plan]
steps = 1
minimality = false  # DROP TYPE is irreversible; post-apply re-plan is empty
golden     = false  # intent/ fixtures opt out of L3 golden

[expect.dep_graph]
enabled = false     # intent/ fixtures opt out of L8

[[expect.intent]]
kind   = "drop_type"
target = "app.status"
reason_contains = []
