[meta]
title     = "DROP VIEW requires an approved intent"
authoring = "intent"
spec_refs = ["views.drop"]

[expect.diff]
contains = ["app.old_view"]

[expect.plan]
steps = 1
minimality = false  # DROP VIEW is irreversible; post-apply re-plan is empty
golden     = false  # intent/ fixtures opt out of L3 golden

[expect.dep_graph]
enabled = false     # intent/ fixtures opt out of L8

[[expect.intent]]
kind   = "drop_view"
target = "app.old_view"
reason_contains = []
