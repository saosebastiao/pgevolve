[meta]
title = "DROP COLUMN requires an approved intent"
authoring = "intent"

[expect.diff]
contains = ["legacy_id"]

[expect.plan]
steps = 1
minimality = false   # intent/ fixtures opt out of L5 minimality — a DROP COLUMN
                     # is irreversible; the post-apply re-plan is empty but we
                     # do not assert it here to keep the fixture focused on L4.
golden = false       # intent/ fixtures opt out of L3 golden

[expect.dep_graph]
enabled = false      # intent/ fixtures opt out of L8

[[expect.intent]]
kind = "drop_column"
target = "app.users"
reason_contains = []
